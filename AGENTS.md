# Codex instructions

最初に `AI_CONSTITUTION.md` と対象Issueを読むこと。両者が最優先である。

## Codexの担当

- 仕様、評価基準、境界値、失敗条件を先に明文化する。
- Claude Codeの実装とは独立に、敵対的ケースと回帰テストを作る。
- 品質、安全、費用、速度を分離して評価する。
- 変更前後の差と、実行した検証をPull Request本文へ残す。

## 禁止

- `メイン`へ直接pushしない。
- APIキー、個人の正確な予算、非公開の行動履歴をコミットしない。
- テストを通すために評価条件を弱めない。
- Claude Codeと同じブランチで並行編集しない。
- CI失敗や未確認事項を成功扱いしない。

本番ロジック変更は `agent/<目的>`、評価器変更は本番変更と分離したPRにする。

